engines:
  bandit:
    enabled: true
    exclude_paths:
      - tests/*
    config:
      ignore:
        - B602  # subprocess call with non-literal argument
        - B603  # subprocess_without_shell_equals_true or similar
